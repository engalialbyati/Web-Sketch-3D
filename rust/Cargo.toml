[package]
name = "websketch-geometry"
version = "0.1.0"
edition = "2021"
description = "WebSketch 3D geometry kernel hot paths, compiled to WASM"
repository = "https://example.invalid"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
